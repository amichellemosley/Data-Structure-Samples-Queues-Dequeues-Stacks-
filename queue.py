class incomingCalls:
    __init__(self):
        self.Queue = ()

def inboundCalls(self):
    #callerID would be their phone number
    if holdingTime == 1:
        call.enqueue(callerID)
        if holdingTime != 1:
            call.dequeue(callerID)
            return callerID, holdingTime
        
