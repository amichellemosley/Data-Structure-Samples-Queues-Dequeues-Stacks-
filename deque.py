class incomingCalls:
    __init__(self):
        self.Deque = ()

def inboundCalls(self):
    #callerID would be their phone number
    if holdingTime == 1:
        call.addFront(callerID)
        if holdingTime != 1:
            call.removeRear(callerID)
            return callerID, holdingTime
    

    